.box {
  width: 100vw;
  height: 100vh;
  background-color: rgba(0, 0, 0, 0.5);
}

.box2 {
  width: 500px;
  height: 300px;
  background-color: $color3;
  position: absolute;
  left: 0;
  right: 0;
  top: 0;
  bottom: 0;
  margin: auto;
}
table{
     border 1px solid $font
     width 80%
     margin 20px auto
     font-size 20px
     color  #000000
    
   tr{
      border 1px solid $font
        
     th,td{
      text-align center
      border 1px solid $font
     }
   }
}